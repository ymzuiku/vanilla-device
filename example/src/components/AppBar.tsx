import React from "react";

export const AppBar = () => {
  return (
    <div
      style={{
        height: 50,
        paddingBottom: "var(--safe-bottom)",
        backgroundColor: "#fff",
        position: "fixed",
        left: 0,
        bottom: 0,
        width: "100vw",
        overflow: "hidden",
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      AppBar
    </div>
  );
};
