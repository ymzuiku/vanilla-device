import React from "react";

export const Header = () => {
  return (
    <div
      style={{
        paddingTop: "var(--safe-top)",
        height: 50,
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      Header
    </div>
  );
};
